import React from 'react';

const Calculate = ({calculateClick}) => {
  return (
     <button className="btn btn-outline-success" onClick={calculateClick} > Calculate </button>
  );
}

export default Calculate
